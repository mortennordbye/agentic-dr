# Fixture for engine/gitops-rewrite.mjs. The copy under source/ is the prod input; the copy under
# expected/ is the golden output — regenerate expected/, never hand-edit it.
#
# A nested, non-YAML file. Two things it pins: the walk recurses into subdirectories, and the
# rewrite is plain text, so a component is not required to be YAML to be regenerated.
allow ctso-prod-weu-hub;
allow 10.100.4.0/24;
deny all;
